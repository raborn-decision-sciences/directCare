% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client_ip.R
\name{extract_client_ip}
\alias{extract_client_ip}
\title{Extract the real client IP from a Shiny session's request}
\usage{
extract_client_ip(session)
}
\arguments{
\item{session}{The Shiny session (its \code{request} field is a Rook-style
environment; same access pattern already used elsewhere in these apps
for \code{QUERY_STRING}).}
}
\value{
The client's IP as a string, or \code{NA_character_} if it can't be
determined (e.g. \code{session$request} unexpectedly missing both
headers) -- callers should treat that as "nothing to gate on", not an
error.
}
\description{
Caddy is the only reverse proxy in front of this app and \emph{appends} to
\code{X-Forwarded-For} rather than replacing it -- the last hop is always the
real connecting IP; anything earlier in the header could be forged by
the client itself (e.g. a request sent with its own
\verb{X-Forwarded-For: 1.2.3.4} -- Caddy tacks the real IP on after it rather
than dropping the forged value). Reading the \emph{first} value instead would
silently defeat IP-based lockout entirely.
}
\details{
Only ever called from each app's own \code{run_app.R}, where \code{session} is
actually in scope (\code{check_credentials_db()} itself, called by
\code{shinymanager::secure_server()}, has no session access) -- lives here
anyway since both apps need it and it belongs alongside the lockout
logic it feeds.
}
