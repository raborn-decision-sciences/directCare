forecast_target <- function(income_summary, overhead_summary,
                             target_income,
                             horizon_weeks = 52) {
  # Given a target net income, returns what weekly revenue
  # is required and projects when current trend hits it
}