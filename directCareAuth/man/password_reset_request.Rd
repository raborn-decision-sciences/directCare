% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/password_reset.R
\name{password_reset_request}
\alias{password_reset_request}
\title{Request a password reset token for an email}
\usage{
password_reset_request(
  con,
  email,
  expiry_minutes = DEFAULT_RESET_TOKEN_EXPIRY_MINUTES
)
}
\arguments{
\item{con}{An open \code{DBI} connection.}

\item{email}{The email address requesting a reset.}

\item{expiry_minutes}{How long the token stays valid. Defaults to 60.}
}
\value{
\verb{list(ok = TRUE, token = <raw token>, practice_id = , practice_name = )}
or \code{list(ok = FALSE)}.
}
\description{
Looks up the practice by email; if found, generates a random token,
stores its hash with an expiry, and returns the raw token for the
caller to embed in a reset-link email (via \code{\link[=send_password_reset_email]{send_password_reset_email()}}).
}
\details{
Callers must not reveal the \code{ok}/\code{FALSE} distinction to the end user --
this function returning \code{list(ok = FALSE)} for an unrecognized email,
silently, is exactly what prevents the reset-request form from being
usable to test which emails have accounts. Show the same generic
"if an account exists, we've sent a link" message either way.
}
