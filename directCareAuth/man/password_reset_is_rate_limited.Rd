% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/password_reset.R
\name{password_reset_is_rate_limited}
\alias{password_reset_is_rate_limited}
\title{Check whether an email has requested too many password resets recently}
\usage{
password_reset_is_rate_limited(
  con,
  email,
  max_requests = DEFAULT_RESET_MAX_REQUESTS,
  window_minutes = DEFAULT_RESET_WINDOW_MINUTES
)
}
\arguments{
\item{con}{An open \code{DBI} connection.}

\item{email}{The email to check.}

\item{max_requests}{Request threshold. Defaults to 3.}

\item{window_minutes}{Lookback window, in minutes. Defaults to 60.}
}
\value{
\code{TRUE} if rate-limited, \code{FALSE} otherwise.
}
\description{
Same pattern as \code{\link[=auth_is_locked_out]{auth_is_locked_out()}} -- counts recent
\code{"password_reset_requested"} \code{auth_events} rows. Checked (and logged)
regardless of whether the email maps to a real account, so the rate
limit itself can't be used to probe which emails have accounts.
}
