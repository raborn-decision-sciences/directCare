# forecast_revenue errors on multi-practice input

    Code
      forecast_revenue(income, method = "linear")
    Condition
      Error in `forecast_revenue()`:
      ! forecast_revenue() requires a single practice. The supplied income_summary contains 2 distinct practice_id values. Filter to one practice before forecasting.

