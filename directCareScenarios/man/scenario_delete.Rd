% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{scenario_delete}
\alias{scenario_delete}
\title{Delete one saved scenario}
\usage{
scenario_delete(con, table, practice_id, id)
}
\arguments{
\item{con}{An open \code{DBI} connection.}

\item{table}{One of \code{"plan_scenarios"}, \code{"dca_calculator_scenarios"},
\code{"dca_forecast_scenarios"}.}

\item{practice_id}{The practice's \code{practices.id}.}

\item{id}{The scenario's row id.}
}
\value{
\code{TRUE} if a row was deleted, \code{FALSE} if no row matched (already
deleted, or \code{id}/\code{practice_id} didn't own it).
}
\description{
Scoped by both \code{id} and \code{practice_id} in the same statement -- not just
\code{id} -- so one practice can never delete another's row even if it guessed
or replayed an id it doesn't own. Works across all three scenario tables
(unlike \code{\link[=scenario_save_json]{scenario_save_json()}}/\code{\link[=scenario_load_json]{scenario_load_json()}}, which are JSONB-
table-only), since deletion needs no knowledge of a table's payload
shape.
}
