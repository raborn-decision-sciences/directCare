% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpret.R
\name{decompose_projection_sensitivity}
\alias{decompose_projection_sensitivity}
\title{Decompose Break-Even Sensitivity by Assumption}
\usage{
decompose_projection_sensitivity(
  assumptions,
  horizon_months = 24L,
  scenario_params = NULL
)
}
\arguments{
\item{assumptions}{Base assumptions list, as passed to
\code{\link[=project_practice]{project_practice()}}.}

\item{horizon_months}{Integer horizon, matching the value used to
produce the projection being interpreted. Defaults to 24.}

\item{scenario_params}{Optional, as passed to \code{\link[=project_scenarios]{project_scenarios()}} --
must match what actually produced the conservative/optimistic
scenarios being decomposed, or the two won't correspond.}
}
\value{
A \code{dcPlanR_sensitivity_decomposition}-classed list with
\code{ramp_spread_months} (integer or \code{NA}; \code{NA} when \code{assumptions} has no
\code{membership_args$ramp_months} to vary, or when either isolated
scenario never recovers within the horizon), \code{overhead_spread_months}
(integer or \code{NA}, same rule), and \code{dominant_lever} (\code{"ramp"},
\code{"overhead"}, or \code{NA} when inconclusive -- neither lever computable,
or an exact tie).
}
\description{
\code{\link[=interpret_projection]{interpret_projection()}} reports how far apart the conservative and
optimistic break-even timelines are, but not which underlying assumption
drives that spread. This isolates the two levers \code{\link[=project_scenarios]{project_scenarios()}}
varies together (membership ramp speed and monthly overhead) by
re-projecting with each one varied independently, holding the other at
its base value.
}
