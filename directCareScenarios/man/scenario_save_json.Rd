% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{scenario_save_json}
\alias{scenario_save_json}
\title{Save (insert or overwrite) a scenario in a JSONB-backed table}
\usage{
scenario_save_json(con, table, practice_id, label, inputs, overwrite_id = NULL)
}
\arguments{
\item{con}{An open \code{DBI} connection.}

\item{table}{\code{"plan_scenarios"} or \code{"dca_calculator_scenarios"}.}

\item{practice_id}{The practice's \code{practices.id}.}

\item{label}{User-chosen slot label.}

\item{inputs}{A plain R list of scalar/small-list inputs.}

\item{overwrite_id}{If supplied, updates that row by id instead of
attempting an insert (label/inputs/\code{updated_at} all replaced) --
required once a save would otherwise exceed the 3-slot cap or collide
with an existing label.}
}
\value{
\verb{list(ok = TRUE, id = <row id>)}, or
\code{list(ok = FALSE, reason = "cap_reached" | "label_collision")} when
\code{overwrite_id} is \code{NULL} and a plain insert isn't possible -- the
caller should show a slot picker and retry with \code{overwrite_id} set.
}
\description{
For Launch Planner and DCA Quick Calculator scenarios -- pure
scalar/small-list inputs, encoded to JSONB via \code{jsonlite}. See
\code{\link[=scenario_save_forecast]{scenario_save_forecast()}} for DCA's full forecast pipeline, which needs
row-level transaction data and is stored as a \code{BYTEA} blob instead.
}
