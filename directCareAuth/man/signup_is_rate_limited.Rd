% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/practices.R
\name{signup_is_rate_limited}
\alias{signup_is_rate_limited}
\title{Check whether an email has attempted too many signups recently}
\usage{
signup_is_rate_limited(
  con,
  email,
  max_requests = DEFAULT_SIGNUP_MAX_REQUESTS,
  window_minutes = DEFAULT_SIGNUP_WINDOW_MINUTES
)
}
\arguments{
\item{con}{An open \code{DBI} connection.}

\item{email}{The email to check.}

\item{max_requests}{Attempt threshold. Defaults to 5.}

\item{window_minutes}{Lookback window, in minutes. Defaults to 60.}
}
\value{
\code{TRUE} if rate-limited, \code{FALSE} otherwise.
}
\description{
Same pattern as \code{\link[=password_reset_is_rate_limited]{password_reset_is_rate_limited()}} -- counts recent
\code{"signup_attempt"} \code{auth_events} rows for the email. Checked (and
logged) regardless of whether the signup itself succeeds, so this
guards against automated account-enumeration/spam via the signup
form's \code{email_taken} response, not just repeated real signups.
}
