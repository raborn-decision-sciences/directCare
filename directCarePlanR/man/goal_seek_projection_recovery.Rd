% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpret.R
\name{goal_seek_projection_recovery}
\alias{goal_seek_projection_recovery}
\title{Find What It Would Take to Recover Within the Horizon}
\usage{
goal_seek_projection_recovery(
  assumptions,
  horizon_months = 24L,
  target_month = horizon_months
)
}
\arguments{
\item{assumptions}{Base assumptions list, as passed to
\code{\link[=project_practice]{project_practice()}}.}

\item{horizon_months}{Integer horizon, matching the value used to
produce the projection being interpreted. Defaults to 24.}

\item{target_month}{Integer month by which recovery should be reached.
Defaults to \code{horizon_months} (the natural target when the base
scenario doesn't recover at all within it).}
}
\value{
A \code{dcPlanR_goal_seek}-classed list with \code{target_month}, \code{overhead}
(a list with \code{achievable} and, when \code{TRUE}, \code{pct_cut} and
\code{new_overhead_monthly}), \code{ramp} (\code{NULL} when \code{assumptions} has no
\code{membership_args$ramp_months} to vary; otherwise a list with
\code{achievable} and, when \code{TRUE}, \code{new_ramp_months} and \code{months_faster}),
and \code{combined} (\code{NULL} when \code{ramp} is \code{NULL}; otherwise a list with
\code{achievable}, and when \code{TRUE}, a smaller simultaneous change to both
\code{overhead} and \code{ramp} -- see \code{.solve_combined_multiplier()}). Each solo
lever is searched independently down to an extreme floor (2\% of
original overhead; a 1-month ramp) -- \code{achievable == FALSE} means even
that extreme isn't enough on its own; \code{combined} is only computed when
both solo levers are independently achievable.
}
\description{
When the base scenario in a projection does not recover its ramp-period
costs within the horizon (see \code{\link[=interpret_projection]{interpret_projection()}}), this finds how
much the overhead level or the membership ramp speed alone would need to
improve for the base scenario to recover by \code{target_month}, holding the
other lever fixed -- the same isolation approach as
\code{\link[=decompose_projection_sensitivity]{decompose_projection_sensitivity()}}, applied as a search instead of a
fixed +/-10\%/25\% perturbation.
}
