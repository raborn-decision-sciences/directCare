% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkout.R
\name{stripe_create_checkout_session}
\alias{stripe_create_checkout_session}
\title{Create a Stripe Checkout Session for a subscription and return its URL}
\usage{
stripe_create_checkout_session(
  practice_id,
  price_lookup_key,
  customer_email,
  success_url,
  cancel_url
)
}
\arguments{
\item{practice_id}{Threaded through as \code{client_reference_id} -- the
only reliable link back to a \code{practices} row once the webhook fires
(a Stripe Customer doesn't exist yet at session-creation time for a
first-time subscriber, so nothing else is stable enough to key off).}

\item{price_lookup_key}{e.g. \code{"pro_monthly"} -- resolved to a live
\code{price_...} id via a live API lookup, never hardcoded.}

\item{customer_email}{Prefills Checkout's email field.}

\item{success_url, cancel_url}{Where Stripe redirects afterward.}
}
\value{
The Checkout Session's hosted URL -- caller redirects the
browser there.
}
\description{
Hosted Checkout, \code{mode = "subscription"} -- see STRIPE_BILLING.md
Part 1 #3 for why this is used instead of a custom Elements form
(card data never touches the app; Stripe owns 3-D Secure/SCA and PCI
scope entirely).
}
