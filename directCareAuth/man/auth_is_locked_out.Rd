% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth_events.R
\name{auth_is_locked_out}
\alias{auth_is_locked_out}
\title{Check whether an email is currently locked out of login}
\usage{
auth_is_locked_out(
  con,
  email,
  max_attempts = DEFAULT_LOCKOUT_MAX_ATTEMPTS,
  window_minutes = DEFAULT_LOCKOUT_WINDOW_MINUTES
)
}
\arguments{
\item{con}{An open \code{DBI} connection.}

\item{email}{The login email to check.}

\item{max_attempts}{Failed-attempt threshold. Defaults to 5.}

\item{window_minutes}{Lookback window, in minutes. Defaults to 15.}
}
\value{
\code{TRUE} if locked out, \code{FALSE} otherwise.
}
\description{
Locked out once it has accumulated \code{max_attempts} \code{"login_failure"}
events within the last \code{window_minutes} -- no separate lockout table,
just a count against the same \code{auth_events} audit log.
}
