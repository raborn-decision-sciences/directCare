% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth_events.R
\name{auth_is_locked_out_by_ip}
\alias{auth_is_locked_out_by_ip}
\title{Check whether an IP address is currently locked out of login}
\usage{
auth_is_locked_out_by_ip(
  con,
  ip,
  max_attempts = DEFAULT_IP_LOCKOUT_MAX_ATTEMPTS,
  window_minutes = DEFAULT_LOCKOUT_WINDOW_MINUTES
)
}
\arguments{
\item{con}{An open \code{DBI} connection.}

\item{ip}{The client's IP to check, or \code{NA} if it couldn't be
determined -- always returns \code{FALSE} in that case (nothing to gate
on), rather than failing open/closed on missing data.}

\item{max_attempts}{Failed-attempt threshold. Defaults to 20 (see
\code{DEFAULT_IP_LOCKOUT_MAX_ATTEMPTS}).}

\item{window_minutes}{Lookback window, in minutes. Defaults to 15.}
}
\value{
\code{TRUE} if locked out, \code{FALSE} otherwise.
}
\description{
Mirror-image of \code{\link[=auth_is_locked_out]{auth_is_locked_out()}}, keyed by \code{ip_address} instead of
\code{email} -- catches a credential-spray attack (many different emails
tried from one source) that the email-based check alone can't see.
\code{check_credentials_db()} gates on either being true, so a login is
blocked if \emph{either} the email or the IP has too many recent failures.
}
