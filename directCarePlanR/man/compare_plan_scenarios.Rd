% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpret.R
\name{compare_plan_scenarios}
\alias{compare_plan_scenarios}
\title{Compare Saved Scenarios' Cash-Flow Recovery Timing}
\usage{
compare_plan_scenarios(scenarios)
}
\arguments{
\item{scenarios}{A list of \code{list(label = , recovery_month = )}, one per
saved scenario. \code{recovery_month} is an integer (the first month
\code{cumulative_net_income} is non-negative) or \code{NA_integer_} if the
scenario never recovers within the horizon it was computed with.}
}
\value{
A character string of narrative text (plain text, not HTML --
matching every other \verb{interpret_*()} function in this package), or
\code{NULL} if fewer than 2 scenarios are supplied (nothing to compare).
}
\description{
Pro-exclusive narrative naming which of a practice's saved plan
scenarios recovers its ramp-period costs soonest. Unlike DCA's
equivalent (\code{directCareAnalytics::compare_breakeven_scenarios()}),
Planner's saved scenarios (\code{plan_scenarios}) store inputs only, not a
results snapshot, so the caller must re-run \code{\link[=project_practice]{project_practice()}} on
each saved scenario's restored assumptions and pass in the resulting
recovery months -- this function itself does no recomputation, it only
narrates months already computed. Every recovery month is on the same
absolute month timeline regardless of the horizon used to compute it
(unlike DCA's weekly-vs-monthly forecast frequency, Planner has no
analogous unit mismatch to guard against).
}
