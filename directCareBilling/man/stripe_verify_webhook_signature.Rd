% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/webhook.R
\name{stripe_verify_webhook_signature}
\alias{stripe_verify_webhook_signature}
\title{Verify a Stripe webhook signature}
\usage{
stripe_verify_webhook_signature(
  payload_raw,
  sig_header,
  secret,
  tolerance_seconds = 300
)
}
\arguments{
\item{payload_raw}{The raw request body, as a single string (e.g.
plumber's \code{req$postBody}) -- not parsed JSON, and not re-encoded.}

\item{sig_header}{The raw \code{Stripe-Signature} header value.}

\item{secret}{The endpoint's \code{whsec_...} signing secret.}

\item{tolerance_seconds}{Reject if \verb{t=} is older than this. Default 300.}
}
\value{
\code{TRUE}/\code{FALSE}.
}
\description{
Stripe signs each webhook with HMAC-SHA256 over \code{"{timestamp}.{raw_body}"},
keyed on the endpoint's signing secret, sent as the \code{Stripe-Signature}
header (\verb{t=...,v1=...}, possibly multiple \verb{v1=} values during a secret
rotation -- matching any one of them is sufficient). Must be computed
against the \emph{raw} request body text, not a re-serialized/re-parsed
version of it -- any whitespace/key-order difference from re-encoding
breaks the signature. Also rejects anything older than \code{tolerance_seconds}
(the \verb{t=} timestamp) to block replay of a captured request.
}
