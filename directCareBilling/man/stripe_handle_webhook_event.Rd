% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/webhook.R
\name{stripe_handle_webhook_event}
\alias{stripe_handle_webhook_event}
\title{Dispatch one verified Stripe event into \code{practices}}
\usage{
stripe_handle_webhook_event(con, event)
}
\arguments{
\item{con}{An open \code{DBI} connection.}

\item{event}{A parsed Stripe event (a nested list, e.g. from
\code{jsonlite::fromJSON(..., simplifyVector = FALSE)}).}
}
\value{
Invisibly, \code{TRUE} if the event changed a row, \code{FALSE} if it
was a recognized-but-no-op type or matched no practice.
}
\description{
Idempotency (see the \code{stripe_webhook_events} table and the webhook
route's own insert-before-processing check) is the caller's
responsibility -- this function assumes the event hasn't been applied
yet. Handles the three events that actually change a practice's
billing state; any other
\code{event$type} is a silent no-op (Stripe sends many event types this app
doesn't act on, and erroring on those would just make retries pile up
for no reason).
}
\details{
\itemize{
\item \code{checkout.session.completed}: first-time subscribe. Looks up the
practice by \code{client_reference_id}, stores \code{stripe_customer_id}/
\code{stripe_subscription_id}, resolves \code{plan_tier} from the session's
line-item price lookup_key, sets \code{subscription_status}.
\item \code{customer.subscription.updated}: plan change, renewal, past_due/
unpaid transition -- refreshes \code{subscription_status}/\code{plan_tier}/
\code{current_period_end} from the Subscription object, looked up by
\code{stripe_customer_id} (not \code{client_reference_id} -- that field only
exists on the original Checkout Session, not later Subscription
events).
\item \code{customer.subscription.deleted}: cancellation took effect --
\code{subscription_status = "canceled"}, \code{plan_tier = "free"}.
}
}
