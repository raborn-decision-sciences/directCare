% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/password_reset.R
\name{password_reset_is_rate_limited_by_ip}
\alias{password_reset_is_rate_limited_by_ip}
\title{Check whether an IP address has requested too many password resets recently}
\usage{
password_reset_is_rate_limited_by_ip(
  con,
  ip,
  max_requests = DEFAULT_RESET_IP_MAX_REQUESTS,
  window_minutes = DEFAULT_RESET_WINDOW_MINUTES
)
}
\arguments{
\item{con}{An open \code{DBI} connection.}

\item{ip}{The client's IP to check, or \code{NA} if it couldn't be determined
-- always returns \code{FALSE} in that case, rather than failing open/closed
on missing data.}

\item{max_requests}{Request threshold. Defaults to 10 (see
\code{DEFAULT_RESET_IP_MAX_REQUESTS}).}

\item{window_minutes}{Lookback window, in minutes. Defaults to 60.}
}
\value{
\code{TRUE} if rate-limited, \code{FALSE} otherwise.
}
\description{
Mirror-image of \code{\link[=password_reset_is_rate_limited]{password_reset_is_rate_limited()}}, keyed by \code{ip_address}
instead of \code{email} -- catches automated reset-request spam cycling
through different emails from one source that the email-based check
alone can't see. Callers should gate on either being \code{TRUE}, same as
login lockout's \code{\link[=auth_is_locked_out]{auth_is_locked_out()}}/\code{\link[=auth_is_locked_out_by_ip]{auth_is_locked_out_by_ip()}} pair.
}
