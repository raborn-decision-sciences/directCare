# filter_gnucash_overhead warns on negative amounts

    Code
      filter_gnucash_overhead(transactions)
    Condition
      Warning:
      1 negative amount(s) detected and flagged as refunds. These will reduce overhead totals in the affected month(s).

