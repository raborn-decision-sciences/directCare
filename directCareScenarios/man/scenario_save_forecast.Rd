% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{scenario_save_forecast}
\alias{scenario_save_forecast}
\title{Save (insert or overwrite) a DCA full-forecast scenario snapshot}
\usage{
scenario_save_forecast(
  con,
  practice_id,
  label,
  bundle,
  source_filename = NULL,
  overwrite_id = NULL
)
}
\arguments{
\item{con}{An open \code{DBI} connection.}

\item{practice_id}{The practice's \code{practices.id}.}

\item{label}{User-chosen slot label.}

\item{bundle}{A plain R list (\code{transactions}/\code{overhead_monthly}/
\code{income_monthly}/\code{inputs}/\code{results} -- see \code{SAVED_SCENARIOS.md} for the
exact shape). Must contain only plain numbers/dates/tibbles, no plots
or other non-serializable objects.}

\item{source_filename}{The originating upload's filename, or \code{NULL} for
a manual-entry-only session.}

\item{overwrite_id}{Same semantics as \code{\link[=scenario_save_json]{scenario_save_json()}}.}
}
\value{
Same return shape as \code{\link[=scenario_save_json]{scenario_save_json()}}.
}
\description{
Bundles both inputs and the computed forecast results -- never just
inputs to recompute-on-load -- into one \code{saveRDS(..., compress = "xz")}
payload. See \code{SAVED_SCENARIOS.md}'s "Why BYTEA" section: this table
carries row-level transaction data (an 11-column tibble with \code{Date}
columns), which JSONB would need custom type handling for with no real
benefit, since nothing needs to query inside this payload from SQL.
Snapshotting results (not just inputs) guarantees a saved comparison
never silently drifts if \code{directCareForecastR}'s forecasting logic
changes later.
}
