% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{scenario_load_json_many}
\alias{scenario_load_json_many}
\title{Load multiple saved JSONB scenarios' inputs in one query}
\usage{
scenario_load_json_many(con, table, ids)
}
\arguments{
\item{con}{An open \code{DBI} connection.}

\item{table}{\code{"plan_scenarios"} or \code{"dca_calculator_scenarios"}.}

\item{ids}{Integer vector of scenario row ids.}
}
\value{
A named list keyed by \code{id} (as character), each element shaped
like \code{\link[=scenario_load_json]{scenario_load_json()}}'s return value. An id with no matching row
is simply absent from the result (not present as a \code{NULL} entry).
}
\description{
Batched form of \code{\link[=scenario_load_json]{scenario_load_json()}} -- one round trip instead of one
per id, for callers (e.g. scenario comparison) that need several
scenarios at once.
}
