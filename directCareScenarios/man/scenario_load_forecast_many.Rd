% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{scenario_load_forecast_many}
\alias{scenario_load_forecast_many}
\title{Load multiple saved DCA full-forecast scenario snapshots in one query}
\usage{
scenario_load_forecast_many(con, ids)
}
\arguments{
\item{con}{An open \code{DBI} connection.}

\item{ids}{Integer vector of scenario row ids.}
}
\value{
A named list keyed by \code{id} (as character), each element shaped
like \code{\link[=scenario_load_forecast]{scenario_load_forecast()}}'s return value. An id with no matching
row is simply absent from the result (not present as a \code{NULL} entry).
}
\description{
Batched form of \code{\link[=scenario_load_forecast]{scenario_load_forecast()}}, for callers that need several
scenarios at once (e.g. scenario comparison, which previously issued one
\code{SELECT} per saved scenario inside an \code{lapply()}). Deserialization
(\code{unserialize()}/\code{memDecompress()}) still happens per row in R -- that
can't be batched at the SQL level -- but the network round trip, the
actual N+1 cost, collapses to one regardless of how many ids are passed.
}
