% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_scenario_slots.R
\name{mod_scenario_slots_server}
\alias{mod_scenario_slots_server}
\title{Save/Load scenario-slot server logic}
\usage{
mod_scenario_slots_server(
  id,
  table,
  get_con,
  practice_id,
  get_inputs_for_save = NULL,
  get_bundle_for_save = NULL,
  get_source_filename = NULL,
  get_dirty_signal,
  on_load,
  extract_dirty_signal = function(x) x,
  is_forecast = FALSE
)
}
\arguments{
\item{id}{Module namespace ID.}

\item{table}{One of \code{"plan_scenarios"}, \code{"dca_calculator_scenarios"},
\code{"dca_forecast_scenarios"}.}

\item{get_con}{A zero-arg function returning a fresh, open \code{DBI}
connection for each action (opened and closed within a single
observer, matching this app's existing \code{db_connect()}/\code{on.exit()}
convention -- never held open across the module's lifetime).}

\item{practice_id}{A zero-arg function (e.g. \code{function() r$practice_id})
returning the current practice's id.}

\item{get_inputs_for_save}{For the two JSONB tables: a zero-arg function
returning the plain R list to save. Ignored when \code{is_forecast = TRUE}.}

\item{get_bundle_for_save}{For \code{dca_forecast_scenarios} only: a zero-arg
function returning the full input+results bundle list (see
\code{SAVED_SCENARIOS.md}). Required when \code{is_forecast = TRUE}.}

\item{get_source_filename}{For \code{dca_forecast_scenarios} only: a zero-arg
function returning the originating upload's filename, or \code{NULL}.}

\item{get_dirty_signal}{A zero-arg function returning a lightweight,
\code{identical()}-comparable snapshot of whatever should trigger
auto-clearing the banner on a real edit -- for the two JSONB tables,
typically the same value as \code{get_inputs_for_save()}; for
\code{dca_forecast_scenarios}, a lighter subset (the Projections tab's own
current input settings, not the full transactions/results bundle).}

\item{on_load}{A one-arg function called with the loaded scenario's
\code{inputs} (JSONB tables) or \code{bundle} (forecast table) once the user
picks one to load -- see the file-level comment above for the
two-phase-load expectation for hosts with dynamic sub-inputs.}

\item{extract_dirty_signal}{A one-arg function applied to whatever was
just passed to \code{on_load()}, producing the snapshot compared against
\code{get_dirty_signal()}'s live readings. Defaults to the identity
function (correct for the two JSONB tables, where the full loaded
\code{inputs} list already matches \code{get_dirty_signal()}'s shape).}

\item{is_forecast}{\code{TRUE} for \code{dca_forecast_scenarios}, \code{FALSE}
(default) for the two JSONB tables.}
}
\value{
\verb{list(loaded_label = <reactive>)}.
}
\description{
Owns the Save/Load modals (including the overwrite-slot picker once a
save would exceed the 3-slot cap or collide with an existing label) and
the "viewing saved scenario" banner state -- see the file-level comment
above for the full design.
}
