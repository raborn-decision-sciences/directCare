% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_pool.R
\name{db_pool}
\alias{db_pool}
\title{Get (creating if needed) the shared connection pool}
\usage{
db_pool()
}
\value{
A \code{pool::Pool} object.
}
\description{
Uses the same connection params as \code{\link[=db_connect]{db_connect()}}. Created lazily on
first call and memoized for the life of the R process -- call this
explicitly once at app startup (see both apps' \code{run_app.R}) to pay the
pool's initial-connection cost before the first real request, rather
than on whichever request happens to arrive first.
}
\details{
\code{minSize}/\code{maxSize} are deliberately modest Phase-3 starting values, not
yet tuned against real traffic -- mirrors \code{mirai::daemons(n = 2)}'s own
"no CPU/memory limit set in docker-compose.yml" reasoning in both
apps' \code{run_app.R}.
}
